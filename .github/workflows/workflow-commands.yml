name: "Workflow Commands"

on: ['push']

jobs:
    my_job:
        runs-on: ubuntu-latest
        steps:
            - name: "group logging"
            run: |
                echo "::group::My Group Message"
                echo "MSg 1"
                echo "MSg 2"
                echo "::endgroup::"
                