name: context-Examples

on:
    push:
        branches:
            - main

jobs:
    my-context:
        runs-on: ubuntu-latest
        env:
            MY_ACTION:${{github.action}}
        steps:
            - name: "My step"
              run: echo "Hello! $MY_ACTION"